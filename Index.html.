<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For Zaki ❤️</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Quicksand:wght@400;600&display=swap" rel="stylesheet">
    <style>
        /* --- CSS STYLES --- */
        body {
            background: linear-gradient(135deg, #fad0c4 0%, #ffd1ff 100%);
            font-family: 'Quicksand', sans-serif;
            color: #5a3e5a;
            margin: 0;
            padding: 20px;
            overflow-x: hidden;
            text-align: center;
            min-height: 100vh;
        }

        h1, h2, h3 {
            font-family: 'Dancing Script', cursive;
            color: #d63384;
        }

        /* Floating Hearts Background */
        .heart-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }
        .heart-bg span {
            position: absolute;
            display: block;
            width: 20px;
            height: 20px;
            background: rgba(255, 192, 203, 0.7);
            transform: rotate(45deg);
            animation: float 15s infinite;
        }
        .heart-bg span::before, .heart-bg span::after {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            background: inherit;
            border-radius: 50%;
        }
        .heart-bg span::before { top: -50%; }
        .heart-bg span::after { left: -50%; }
        
        @keyframes float {
            0% { transform: translateY(100vh) rotate(45deg) scale(0.5); opacity: 0; }
            50% { opacity: 1; }
            100% { transform: translateY(-10vh) rotate(45deg) scale(1.2); opacity: 0; }
        }

        /* Container */
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding-bottom: 50px;
        }

        /* 1. Countdown */
        .countdown-box {
            background: rgba(255, 255, 255, 0.8);
            padding: 15px;
            border-radius: 15px;
            margin-bottom: 30px;
            box-shadow: 0 4px 15px rgba(214, 51, 132, 0.2);
            animation: pulse 3s infinite;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.02); }
            100% { transform: scale(1); }
        }

        #timer {
            font-size: 1.5em;
            font-weight: 600;
            color: #d63384;
        }

        /* --- Moving Picture Belt --- */
        .belt-section {
            margin-top: 40px;
            overflow: hidden;
            width: 100%;
            position: relative;
        }

        .belt-title {
            font-size: 2.5rem;
            margin-bottom: 15px;
        }

        .slider {
            background: rgba(255, 255, 255, 0.4);
            padding: 15px 0;
            display: flex;
            width: 100%;
            overflow: hidden;
            border-top: 2px solid rgba(255,255,255,0.6);
            border-bottom: 2px solid rgba(255,255,255,0.6);
        }

        .slide-track {
            display: flex;
            width: calc(280px * 8); /* Width * number of slides */
            animation: scroll 20s linear infinite;
        }

        /* This creates the smooth infinite loop */
        @keyframes scroll {
            0% { transform: translateX(0); }
            100% { transform: translateX(calc(-280px * 4)); }
        }

        .slide {
            height: 250px;
            width: 250px;
            margin: 0 15px;
        }

        .slide img {
            height: 100%;
            width: 250px;
            object-fit: cover;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            border: 4px solid white;
            transition: transform 0.3s;
        }

        .slide img:hover {
            transform: scale(1.1);
            border-color: #d63384;
        }

    </style>
</head>
<body>

    <div class="heart-bg" id="heart-bg"></div>

    <div class="container">
        <h1>For Zaki ❤️</h1>
        
        <div class="countdown-box">
            <h3>Days with you:</h3>
            <div id="timer">Loading...</div>
        </div>

        <div class="belt-section">
            <h2 class="belt-title">Our Memories</h2>
            <div class="slider">
                <div class="slide-track">
                    <div class="slide"><img src="https://via.placeholder.com/250" alt="1"></div>
                    <div class="slide"><img src="https://via.placeholder.com/250" alt="2"></div>
                    <div class="slide"><img src="https://via.placeholder.com/250" alt="3"></div>
                    <div class="slide"><img src="https://via.placeholder.com/250" alt="4"></div>
                    <div class="slide"><img src="https://via.placeholder.com/250" alt="1"></div>
                    <div class="slide"><img src="https://via.placeholder.com/250" alt="2"></div>
                    <div class="slide"><img src="https://via.placeholder.com/250" alt="3"></div>
                    <div class="slide"><img src="https://via.placeholder.com/250" alt="4"></div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // --- Floating Hearts Logic ---
        const heartContainer = document.getElementById('heart-bg');
        for (let i = 0; i < 15; i++) {
            const heart = document.createElement('span');
            heart.style.left = Math.random() * 100 + '%';
            heart.style.animationDelay = Math.random() * 5 + 's';
            heartContainer.appendChild(heart);
        }

        // --- Timer Logic ---
        // SET YOUR START DATE HERE (Year, Month index 0-11, Day)
        const startDate = new Date(2023, 0, 1); 

        function updateTimer() {
            const now = new Date();
            const diff = now - startDate;
            const days = Math.floor(diff / (1000 * 60 * 60 * 24));
            document.getElementById('timer').innerText = days + " Beautiful Days";
        }
        setInterval(updateTimer, 1000);
        updateTimer();
    </script>
</body>
</html>
